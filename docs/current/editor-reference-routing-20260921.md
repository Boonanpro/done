# 上流の参考検索・映像情報の拡充（2026-09-21）

## 実装と反映

- 元のURL索引1,178本は維持。全26主ジャンルから52本を追加分析した。
- 既存分と合わせ、検索用の映像観察は3本から51本、代表区間は185区間になった。既存のGPT6ローンチ詳細分析は別に保持。
- GeminiのURL動画分析を3並列で実行。ローカルGPUや動画生成は使っていない。今回の52本の処理終了まで454秒。各ファイルの旧`seconds`には待ち行列時間が含まれるため、動画尺・API処理時間として扱わない。以後は待機と分析時間を分離する。
- キャッシュ再利用、区間の数値・動画尺検査、監査で意見が分かれた記録の除外、原記録保存、原子的な公開を追加。
- 5本を別の動画分析呼び出しで再確認。3本で記述の相違を指摘されたため除外。既存1本にも動画尺を超える区間があり除外。残り全件の内容を人間が確認したわけではない。
- 検索では用途と見た目から複数ジャンルを選び、弱い副ジャンル関連も残す。最初の好み不明の提案は従来の全件検索。好みがある場合に絞り込みを使い、不確実・削減効果小・該当なしでは全件へ戻す。
- 比率・タイムライン・動画制作は変更していない。

## 比較結果

6種類の依頼を各3回、同じデータで全件方式と比較。種類はAI紹介、普通の部屋の実写、宇宙風3D、編集解説、結婚式アニメ、料理。これは検索の試験であり、音声入力から画面表示までの試験ではない。

| 指標 | 結果 |
| --- | --- |
| 全件検索の中央値 | 1,210.5 ms |
| ジャンル検索の中央値（フォールバック含む） | 889.5 ms |
| 待ち時間の減少 | 約26.5% |
| 全件検索の結果が絞り込み後の検索対象に残った数 | 41 / 41 |
| 自動テスト | 27件合格 |

結果の順位や選ばれる動画自体は変動する。41/41はユーザーの好みへの正解率ではない。初期の強い副ジャンル閾値では、料理の短編映画を落とした。弱い関連も残すことで今回の取りこぼしは解消した。広い検索ではジャンル選択の追加往復で遅くなる例があり、常に高速化できるわけではない。

## 実際のアプリ確認

稼働中の音声がないことをログで確認し、Core管理APIでsandboxを更新。PID70084、healthy。Core・フロントエンドは再起動していない。

- 本番APIで普通の部屋の人物の参考を要求：新規観察済み `yt-VojDzHaciKQ` が選ばれた。API全体2,784ms。この依頼ではジャンルによる削減効果が小さく全件へ戻った。
- 本番APIで落ち着いた料理の縦動画を要求：ジャンル検索の実行を確認、API全体1,474ms。ただし適切な比較根拠が足りず提示なし。これを提案成功とは数えない。
- 音声会話・動画再生までの通し試験は今回は行っていない。参考データと検索の改善に限定した。

## 残る上流の課題

料理の縦動画のように、題材は合っても構図・テンポ・縦横比まで確かめられる参考が不足する場合がある。51本の観察を完成扱いせず、不足している組み合わせを追加収集・分析する。ジャンル確率だけで品質やユーザーの満足を保証しない。代表区間の検索データはできたが、その区間を会話に応じて自動再生するUIは今回追加していない。

証拠：`scratch/reference-routing/results.json`、`production-decision.json`、`production-cooking-decision.json`、`scratch/discovery-observations/quality-report.json`、`audit-results.json`。
