# 上流の確認・修正（2026-09-22）

対象は参考を相談して方向を決める工程。下書き制作、再利用部品の量産、音声モデル移行は変更していない。

## 発見と変更

- `editor_consultation.questions` の実際の Choice に ask/propose がなかった。一方で update、ブラウザーへの guidance、テストは両方を扱っていた。テストは実際には選べない propose を模擬していた。
- 選択肢追加だけでは実Jevは7ケース中3件しか期待する判断をしなかった。「会話」の範囲が参考提案や確認と重複していた。競合する長い指示を置き換え、直接の質問を優先しつつ、具体的な制作相談では自発的に比較、方向が決まれば不足条件の確認、条件が揃えば次の具体化の提案を選べるようにした。
- 「ask/propose が実際の選択肢に存在する」回帰テストを追加。
- 参考の内容適合性とURL可用性チェックは独立なので並行化した。両方の除外が効き、互いを待たず開始することをイベント同期のテストで確認。候補削除や確認省略による高速化ではない。

## 検証

`scripts/check_upstream_consultation.py` は音声接続を開かず、実Jevを使用する。

- 曖昧な相談、具体的な映画、製品紹介、条件回答後の提案、条件不足、説明だけの質問、方向転換の7ケース。
- 判断単独は7/7期待どおり、約0.22〜0.85秒。
- `--route` は実際の `editor_visual_decision.decide` と全参考索引に接続。7/7、比較3ケースすべて参考IDが返り、バックエンド制作への委譲なし。
- 4並列の参考検索を含む応答は3.749/8.349/8.431秒。会話の判断だけの4ケースは0.231〜1.069秒。
- 8並列の試験は4.200/10.572/10.139秒で悪化したため撤回。最終コードは4並列。単発比較なので原因を提供側の制限と断定しない。
- 関連テスト61件合格。8並列撤回は既に検証した4並列への復帰。

これは実音声・ブラウザー表示までの試験ではなく、候補の映像品質やユーザーの満足への合格でもない。

## データと残り

登録6,753本、映像観察があるもの103本、33ジャンル。今回増やした本数はゼロ。
各ジャンルに観察済みはあるが2〜5本程度で、細かいテイストの組み合わせを網羅したとは言えない。
全件検索では約86〜88バッチが発生し、広い検索の待ち時間が残る。

上流の次の検証は好み・拒否・方向転換が次の実物へ反映される連続会話と、実画面への提示。
その結果とデータのカバレッジを照合して観察情報を補う。下書きの再現テストへは移らない。

## 反映

ソース保存・別プロセスで検証済み。稼働中の音声接続を検出したためsandboxを再起動していない。
したがって本番反映済みとは報告しない。Core、frontend、制作ジョブには触れていない。

証拠: `scratch/upstream-consultation-20260922/` の before.json、results.json、route-four-workers.json、route-eight-workers.json。

## 追記：反映と相談メモの表示

ユーザーが音声終了を知らせた後、接続・制作ジョブなしを確認し、Core管理APIでsandboxのみ再起動。PID13620、healthy。上記Python変更を反映した。

`editor-live.js` に一時的な読み取り専用「相談メモ」を追加。初期表示、右上で開閉できる。現在の作品に紐づく実際のlocalStorageの相談メモを表示し、0.5秒間隔でローカルの変化を確認する。API呼び出し・会話への追加指示は発生しない。

表示項目は現状の内部項目に合わせ、作りたいもの／目的・見る人・公開先／尺・素材など／支持した方向。記録のあることを全条件確認済みとは扱わない。次の判断と未提示の参考依頼も表示する。別作品への移動ではその作品のメモへ切り替わる。

稼働中の8000番が配信するページをHeadless Edgeで確認。メモ更新、4項目の未記録表示、別作品への切替、開閉、390px幅を検証。スクリーンショットも目視。JavaScriptエラーなし。これは記録表示の試験であり、音声での自動抽出の正確性を保証するものではない。既存音声JSの48テストも合格。

証拠: `consultation-inspector.png`、`consultation-inspector-mobile.png`。再現スクリプト: `scripts/check_consultation_inspector.py`。
