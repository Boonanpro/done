# 作業受付前の待ち時間

今回の174秒待ちの原因は未確定。再起動防止はユーザー指示により対象外。

## 読み取りのみの計測

同じDone部屋に対するcommand_center searchは1.094秒、read(20件)は1.438秒。
検索語の並列検索は既にasyncio.gatherで実装されている。

通話前の履歴と当該発言をVoiceCodexへ渡し、検索・状態取得だけを許す試験を実施。
delegate_to_danが選ばれた時点で止め、取消やブラウザ操作は実行していない。
検索結果には現在の記録も含まれるため、当時の完全再現ではない。

- 基準: 24.937秒で委譲。モデル処理計約21.780秒、道具約3.157秒。
- 指示を軽くする候補: 29.281秒で委譲。短縮確認できず変更は戻した。
- 証拠: `.tmp/voice-intake-readonly-before.json`、`.tmp/voice-intake-readonly-replay.json`。
- 複数回の統計ではなく、3分待ちを再現できていない。改善率の主張には使わない。

## 保存・反映

SSEのcompletedを受け取ってからHTTP EOFを待つ処理を削除。
完了フレームで直ちに戻り、読み取りをcancelする。処理を再実行しない。
HTTPストリームが閉じない試験を含め、共有通信・スマホ統合35テスト合格。
スマホ1.0.25 / code23へインストール済み。通話は起動していない。
この修正が今回の174秒の原因だった証拠はない。

端末にbackend_request / backend_headers / backend_completed / backend_failedの
時刻・所要時間を記録する処理も反映。入力本文は記録しない。
VoiceCodex側の入力受付・モデル待ち・道具要求・完了の計測は保存済み、11テスト合格。
サーバー側は未反映。Sandbox配下に稼働エディターがあり再起動しなかった。

次は端末とサーバーの同一要求の時間を照合し、モデル処理と通信待ちを切り分ける。
Jev追加・モデル並列化による全体速度向上、精度向上は未検証。
