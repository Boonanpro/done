## 2026-09-07 見学用音声テスト：下書きから部分修正・書き出し

- observer-8c691af8で、参考選択→15秒のカフェ下書き→入口だけ明るさ修正→末尾字幕の言い換え→書き出しまで実行。合成したユーザー音声を実Realtime入力へ送り、実Astra・実生成サービス・実ネイティブエディターを利用。本人のマイクや環境雑音の品質試験ではない。
- 下書き8a584d8dは約8分45秒。Seedance 2.0の実生成は約162秒、残りは制作判断・配置・検品・修正。5つの映像クリップ、リンク音声、末尾字幕。初稿制作の総時間はまだ短縮余地がある。
- 試験で検出したアプリ不具合を修正：字幕を先に置くと後から追加した映像が上に乗る／IDなしの旧レーンを全て同一視して部分編集を拒否する／明るさの引数説明と現在の補正値が会話側から読めない。新規レーンにはIDを付与し、旧データは変更せず比較可能にした。不明なgradeキーは既存補正を消さずエラーを返す。
- 入口0〜4.8秒のevのみ0.4→0.65。他のシーケンスデータは完全一致。ネイティブ合成画像を前後比較し、指定箇所1.5秒は変化、指定外4.9秒・13秒はピクセル一致。scoped-edit-verification.jsonに証跡。修正後の再依頼は過去の失敗を受けて再委譲されたため、明るさの実音声即時編集の速度は未証明。
- 次の「最後の字幕の一息をひらがなにして」は直接timeline_editで実行。発話終了04:15:11.164 UTC→保存04:15:16.754、5.590秒。字幕以外のデータは完全一致。
- 書き出しc05c6fa7は約8秒で1920×1080、音声付き15秒MP4を生成。ただし完了通知へ出力情報が欠落して「まだ進行中」と誤報。保存前の制作報告と現在の保存状態を分離し、出力ファイル情報も通知へ渡すよう修正。再書き出し64ff87e2は成功し、04:18:19.880 UTCに実音声で「書き出しは完了しています」と報告。
- 関連Python30件、ブラウザーの履歴・再接続・通知データの回帰試験を通過。試験用音声送信器にも連続無音入力を追加（発話後に音声グラフが止まりVAD終端が来ない問題を修正）。
- 作品全般の完成度を保証する試験ではない。初稿制作の所要時間、会話の重複した前置き、参考候補の適合度、実マイクでの自然な会話は引き続き評価対象。

## 2026-09-07 参考検索を制作処理から分離

- search_references を音声/テキスト共通の直接ツールとして追加。YouTube公開検索結果の構造化データをHTTP取得。1〜3検索を並行し、戻った候補をContentsLockでpresentationへ保存。最終の文章報告を待たず表示。追加のLLM検索APIやCLI、MCP起動、編集draft、デスクトップブラウザーは経由しない。
- 名前・URLは実際の検索結果から取得。動画本編を検品したとは扱わない。サイトの構造変更/取得制限/検索不成立は明示的な不成立として返し、URLや内容を捏造しない。
- 新しい検索と指示取消を確認して古い結果の上書きを防止。部分成功でも候補は表示。
- observer-8c691af8 の実機音声テスト: 03:49:03.863 UTC 発話再生終了 → 03:49:07.707 search_references → 03:49:08.498 候補表示。発話終了から4.635秒。3検索の取得/保存は740ms、最初の保存は583ms。以前の289秒は全候補の視聴確認も含む別工程であり、同一品質の処理が同倍率で高速化したという測定ではない。
- 関連テスト17件通過。実機で候補カードの表示とカード内での動画再生を確認。新経路の追加検索API料金はなし（会話モデルの既存API料金は発生）。
- 続く音声指示で15秒下書き制作 8a584d8d-4ceb-49b9-ab8b-975abe1c9fed を開始。目的・視聴者・好みが制作指示に含まれることを確認。完成物・部分修正・書き出しの評価は上記。

## 2026-09-07 タイムラインの高さが増える問題

timeline_uiが下部の余白を固定24pxで確保していたが、horizontalの最小高さとitem_spacingの合計がこれを超え、パネルの最小サイズが毎フレーム増えていた。スクロールバー行と現在のspacingから残り高さを算出するtimeline_body_heightに変更。160/260/420pxの各初期高さで240フレーム連続描画してサイズ不変のeguiテストを通過。

見学用の実機テスト補助 scripts/editor_observer.py を追加。隔離ルームを作成して実際のネイティブウィンドウを開く。Windowsの日本語音声をRealtimeの音声入力とローカル再生へ同時に送り、実応答・参照・制作状況を読む。モデルや制作結果はモックしない。人のマイクの音響特性・騒音は別途の実地検証が必要。

## 2026-09-07 音声接続・履歴・待機の修正

- 同じルームのライブラリ移動で制作対象を一時的に失っても接続を維持。作品切り替えでは編集対象のみ更新し、会話は維持。ルーム間の履歴は分離。
- 会話テキストと直前の指示参照を端末に保存し、画面再生成・接続更新から復元。直近200発言を保持（無制限の長期記憶ではない）。通常チャットは混ぜない。
- 切断理由を記録。切断音と表示、最大3回の再接続。ユーザーによる停止は復帰より優先。マイクデバイス終了・通信失敗も検知。
- 待機中は返答前に発話の宛先をモデルで判断し、独り言なら無言を維持。ユーザーからの質問で応答に戻る。
- list_assetsの説明を未配置動画も含むライブラリ検索として明確化。形式の具体案・検討中の候補の扱いを会話指示に追加。
- 検証: ブラウザーの画面遷移・履歴復元・再接続成功/失敗/停止テスト通過。関連Python 21件通過。実Realtimeモデルへテキスト入力し音声出力する隔離テストで、素材一覧、再接続後の提案、独り言で待機、直接の質問で再開を確認。素材・制作担当の応答はfixtureであり、実素材の理解や通し制作の品質を証明するものではない。
- 残課題: 実際のマイク入力での再評価、前置きや不要な提案の冗長さ、実素材を調査した上での能動的な提案品質。過去に消えたブラウザー履歴の自動移行は行っていない。

# 制作体験の完成条件

## 2026-09-07 音声から始める制作画面

- Apple Human Interface Skills（非公式HIG整理）を導入し、余白・抑えた色・情報の優先順位・動きの役割を設計へ適用。
- 制作ライブラリの旧プロンプト・生成モデル選択フォームを撤去し「ダンと制作」へ統一。全面の会話画面から新しい作品を開き、音声接続。編集画面との切り替えでは同じWebViewと音声接続を保持する。
- 音声エージェントを抽象的な球体で表し、聞く・考える・話す状態と実際の音量に反応。会話履歴・テキスト入力・編集操作は引き出しに配置。動きを減らす設定に対応。
- present_referencesを音声窓口と制作担当へ追加。確認済みのHTTPS参考動画・画像を1〜4件提示し、YouTube/Vimeo/直接動画の指定区間を表示。元ページへの導線と候補選択を用意。制作担当の提示もポーリングで同じ画面へ反映。
- 参考例は動画のタイムラインを変更せず作品に保存。提示中の候補とマウスで示した候補を会話へ渡す。参照サイトが埋め込みを許可しない場合は元ページで確認する。
- ライブラリで保存が進まず新規作品への移動が阻まれる不具合を修正。読み込み時の正規化・手編集の保存をライブラリでも処理する。
- この変更で作例コンポーネントの大規模カタログを導入したわけではない。参考提示の道具と体験を接続した段階。エディター内部の全機能・全画面のデザイン刷新完了を意味しない。
- 実機検証: scripts/test_editor_stage_native.py、stage-native-3e43ab3e。無音の仮想マイクでライブラリ→新規作品→実WebRTC接続→編集→全面へ戻る間、同一接続を保持。共有リポジトリの実MP4がWebView2上で1920x1080、15.061秒として読み込めることも確認。Playwright付属Chromiumでは同MP4の再生に失敗したため、実アプリのWebView2で検証した。
- UIの参考区間・引き出し・狭い画面の表示検証に合格。関連Python21件合格、ネイティブreleaseビルド成功。本人による会話の自然さ・見た目の合否は実地評価に残る。

## 2026-09-07 会話と画面・制作状況の分離

- 毎発言のUI詳細・合成画像の自動投入を廃止。古い確認画像も次の発言で外す。画面と指差しの情報はread_editor_context、実物はtimeline_frameで必要時に取得。制作状況はユーザー発言ではなく、取得時刻付きの背景情報として渡す。
- 過去のunfinished_workを会話へ自動投入しない。実行中ジョブの担当発言・質問と直近の終了結果を取得。keepaliveで担当の進捗文が押し出される不具合を修正。
- 質問待ち中の全発言を/answerへ送る処理を撤去。会話モデルが文脈を判断しanswer_questionを呼んだ場合だけ同じ仕事へ回答。通知待ちの質問が更新・回答済みなら古い音声通知を破棄。
- 静かにしてという依頼をset_voice_notificationsで扱い、制作を止めず音声通知を停止。次の質問には応答でき、通知は依頼で再開。返答待ちだからという理由で質問を繰り返さない。
- scripts/test_editor_conversation_context.pyで実Realtime 2.1へテキストで試験発言を入れ、音声出力を検証。制止→質問の意味への問い返し→明示回答→ログイン進捗の4ターンで、画面実況なし、誤転送なし、同じ仕事への回答、最新進捗の説明を確認。本人マイクによる雑音・発話認識の試験とは区別する。前置きや一般論が残る応答はあり、自然さの全問題が解消したとは評価しない。
- ブラウザーでsystem背景情報・画像自動取得なし・回答ツール・通知再試行を確認。関連Python24テスト合格。

## 2026-09-07 通常のDan実行基盤へ統合

- エディター専用のCLI起動と制限的な制作プロンプトを廃止。通常のDanと同じprocess_message_cli、基本指示、接続サービスを使い、制作モデルはgpt-6-astra。タイムラインの下書き編集・差分保存は維持。
- 履歴はeditor_<room>_<content>の専用セッションで永続化。通常チャットへ投稿せず、通常チャットの履歴も自動投入しない。制作担当はread_project_chatで必要時に参照可能。エディター履歴は既存のconversation_historyと専用セッションに保存。
- 制作担当のask_userで質問を表示・音声通知し、音声またはテキストの回答を同じ実行中の仕事へ返す。回答のたびに新しい制作担当を起動しない。
- report_resultで依頼達成・本人操作待ち・進行不可を区別し、会話の状態取得と通知にも結果を渡す。単にCLIが終了したことを依頼達成として扱わない。
- 音声通知の一時的な失敗を最大2回再試行。発話中の通知待機、現在の仕事だけのカード表示を維持。音声窓口の長い手順指示を整理し、制作・調査の判断は通常の制作担当へ渡す。
- 実Astraで指定字幕のみの編集を確認（editor-astra-0f45bcd1 / draft 341dd6eaef6e）。実Astraで質問→回答→同じ仕事の続行→次の会話で回答を記憶することを確認（editor-dialogue-b86526a4）。実Realtimeで制作終了の発話を確認。ブラウザーで質問への回答と通知再試行を確認。関連Python26テスト合格。
- この検証は任意の動画が意図どおり完成する保証ではない。HeyGen実アカウントの準備完了も意味しない。新しい実行基盤を使った本人の自然な制作会話で、残る体験上の問題を評価する。

## 2026-09-07 外部サービス準備の委譲修正

- 会話側にもAstraの検索・ブラウザー・PC操作・接続準備の能力を明示。過去の接続待ちは実行不能の判定ではなく、調べて進める残作業として扱う。
- delegate_editにtask_kind=prepareを追加。音声・テキストともクリップ未選択で準備作業を依頼可能。制作側は空の編集範囲でタイムラインを保持する。
- 実会話モデルに今回のHeyGen接続待ちの会話を再現し、prepareで調査・ログイン等の準備を委譲することを確認。ツール実行はテスト用の模擬応答で、実アカウントには変更していない。
- 関連17テスト合格。未選択での準備受付、原発言の保持、制作下書きの編集範囲保持を含む。

## 2026-09-07 会話・通知修正とAstraへの切り替え

- 制作・編集担当の既定値をgpt-6-astraへ変更。以前の部屋設定はfable。音声会話用Realtimeモデルは別役割として維持。
- 会話用project_statusはactive_count/active_jobsと過去の終了結果を分ける。過去のエラーにある「実行中」を現在の状態として混ぜない。確認の前置きや完成留保を重ねる指示文を整理。
- 発話終了だけで応答を開始せず、文字起こしを受け取ってから応答。空なら会話ターン・編集を開始しない。文字起こしをbeginへ渡し、制作担当にも原発言を保持。
- カードはqueued/runningのみ表示。未完了・失敗・HeyGen待ちは常駐表示から除外。作り直しの経緯UIは追加しない。
- 制作終了時は音声接続・マイク有効中に結果を一度だけ発話。発話中・応答中は待機する。古いsummaryの途中発言を読み上げず、依頼と反映結果から報告する。
- 制作CLIの正常な終了イベントがない場合は下書きを完了扱いにせず、保存済み成果から継続。Codex経路でも道具の開始・終了を活動記録へ反映。
- 実編集検証: editor-astra-292e3a0d / draft b90a78e813fd。既定Astraが指定字幕だけを変更し、他のクリップ・属性保持を完全比較で確認。
- 実Realtime検証: 空発話で応答なし、現在のカードだけ表示、発話中の完了通知待機、実際の音声による完了報告、重複なし。発話「背景4カットが白黒の昭和レトロな映像に変更され、タイムラインに反映されました」を記録。
- 実モデル会話＋模擬制作ツールで「ないならできるやん。作れよ。」を再現。状況確認の反復なしでread_skill→set_project_scope→save_brief→delegate_editへ進行。外部生成はこの会話テストでは行わない。
- 関連Python32件合格、JavaScript構文確認。スクリプト: test_editor_astra.py / test_editor_conversation_fix.py / test_editor_direct_request.py。

## 実地テスト時の指摘記録（上記で対応）

- 過去の失敗の「別ジョブ実行中」を現在の実行状態として誤認する。現在の仕事と過去の記録の渡し方を修正する。
- 状況確認を繰り返して実行を遅らせる会話と、確認・留保を読み上げる指示文を整理する。
- 空の文字起こしでも応答を開始する処理を修正。咳払い等で不要な長い返答を始めない。
- カードは現在実行中の作業のみ。過去の失敗・未完了・HeyGen待ちを常駐表示しない。再生成の経緯表示は追加不要。
- 音声接続中の制作終了を自発的に短く知らせる。ユーザーの発話中は割り込まず、同じ終了を重複通知しない。失敗なら具体的な結果を知らせる。終了情報を会話へ追加するだけで発話を開始していない現実装が原因。b18f8da8-a21b-4f2b-8dc6-0ee45bb51e9bは19:27:20 JSTにdone/committed=trueだが、その後の音声報告はログにない。
- 同ジョブの最終summaryが「ドリップのカット…作り直します」という途中の発言で終わっている。音声通知はこのsummaryをそのまま読まず、制作担当の終了判定・実際の反映結果・残作業の整合も調べる。

## キーボード操作の追加修正

- チャット入力欄へのfocusinからWebViewのfocusを再要求する往復を削除。クリックで決まったDOM入力位置をアプリから移動させない。
- 通常のSpaceはチャット入力欄・各ボタン・ネイティブの文字入力欄でも再生停止へ統一。IME変換中と修飾キー付きの入力は維持し、空白はShift+Spaceで入力可能。押しっぱなしの連続切替を抑止。
- scripts/test_editor_keyboard.pyで文字入力、各コントロールからのSpace、Shift+Space、変換中のガード、改行をブラウザーで検証。Windows実機のIME操作感は本人の再確認対象。

## 2026-09-06 追加実装：会話から実制作を継続する

- エディターの制作担当にも通常のDanのブラウザー・接続サービス・調査用の道具を渡す。実際の制作担当でHeyGenへのアクセス、保存済み接続情報の確認、本人音声の元ファイル探索を実行できた。HeyGenは接続情報が見つからずブラウザーも未ログイン。使うアカウントの回答待ちで、HeyGen音声の生成・差し替えは未完了。
- 元の発言を制作指示に保持し、必要に応じて会話履歴を読む道具を追加。制作中の追加指示は同じ仕事に保存し、次のタイムライン操作前に受け取る。受付と実際の反映は別の状態として表示する。
- 時間切れでも下書きと生成素材を残し、同じ下書きで最大2回セッションを継続する。明示的な再開にも対応。競合時は保存済みの手編集を上書きしない。無期限の自動完遂を保証するものではない。
- 音声は期限前・期限到達時に再接続し、発言履歴とマイク状態を引き継ぐ。実WebRTC接続で、マイク有効のまま再接続して履歴・マイクを維持することを確認した。本人による長時間の自然な会話テストは別途必要。
- 実際の道具の開始・終了・失敗から進行カードを表示。素材制作・編集・検品をアニメーションで示し、対象クリップにも動く枠を表示する。失敗後の回り続ける表示を止め、接続準備などの保留事項を表示する。
- Higgsfieldの入力画像URLを生成動画として保存していた問題を修正。出力URLの選別と動画の実長検証を追加。Gemini Omni Flash 1.1を正式なモデル識別子で接続し、元画像を参照して4本の動画を実生成した。
- コーヒー作品の背景4カットを実動画へ差し替え。生成内の不自然な切り替わりを避ける参照範囲・速度を調整し、エディターの合成器で15.2秒を書き出して検品した。自動検品の誤認もあり、実フレームと動きの数値を照合した。音声・字幕など対象外のクリップを保持して本番作品へCAS反映。draft: 829c2607520d。バックアップ: recovery/before-omni-backgrounds-1788685142.json。確認用動画: uploads/editor-recovery-check/coffee-omni-1.1.mp4。
- 関連Pythonテスト35件合格。別途ワークフロー・速度のテストも合格済み。JavaScript構文検査、ネイティブのチェック・リリースビルド成功。実ブラウザーで進行・失敗・アカウント待ち表示を確認。利用中のエディターを終了させず更新を保留し、閉じられた後に自動配置。配布先と今回のビルドのSHA256一致を確認済み。
- 残る評価：HeyGen接続と音声の実生成、ユーザー本人による音声での連続修正・待ち時間の分かりやすさ・任意の新題材の完成品質。今回の合格を理想体験100%の達成とは扱わない。

2026-09-06 ユーザー承認: 個別の不具合修正で止めず、相談から編集可能な下書き、会話での部分修正、検品、書き出しまで実装・検証する。

## 合格条件

- 吉川: 比率と配置を復旧し、保存・再読込で再発しない。画像の説明との対応と既存テイストを確認する。重複映像は元素材との比較で修正する。
- 新規題材: 意図とテイストの相談から、映像・音声・字幕を含む通しの編集可能な下書きを作り、部分修正して書き出す。
- 会話側は必要に応じて素材、台詞、前後の映像、編集履歴、実行状況とモデルを読める。分からないまま処理中・完成と言わない。
- 複数の依頼・保留事項・委譲した処理を作品単位で記録し、パネル再接続後も追える。
- 既存作品への追加では参照画像を使い、前後の映像・音声を確認する。毎回の確認質問や作品の表現を制限する固定規則は追加しない。
- 実機テストは独立した作品で行う。利用中の画面にOSキーやフォーカス操作を送らない。無断の再起動をテスト手順に含めない。

## 実装・検証記録

- 追加承認: 既存の画面・機能を残すことは目的にしない。必要なら大幅に作り直す。通常の手編集（素材取り込み、分割、移動、音量、字幕、コピー、Undo/Redo、保存、再開、書き出し）も完成条件に含む。
- 新規実地経路: assistant-complete-test-8e96bdba / a65ccfdd-6304-4dee-9fa4-c06d79ab921c。相談→本人声・4画像・5字幕の14.2秒下書き→選択字幕のみ縮小→1920x1080書き出しに成功。品質レビューは別途記録。
- 制作中のAPIモジュール読み込みが実行中ジョブを誤って失敗にした問題を修正。復旧はサーバー起動時のみ、他プロセスの生存を確認する。
- 画像生成のresult_url読み取りを修正。参照素材の出力と取り違えない回帰テストを追加。
- 通常操作: Ctrl+C/V、Ctrl+S、Ctrl+Shift+Z追加。A/Vリンクを保った貼り付けと既存クリップの保持をテスト。保存時は進捗等の非競合変更を統合する。
- ネイティブ19件・Python44件のチェックに合格（2026-09-06）。全体の映像品質・実際の音声操作の合否は別に確認する。

- 調査済み: 制作完了時の二重保存がtimeline.formatを欠落させる。比率復旧操作がジオメトリまで変換していた。
- 既存のサイズ変更・範囲保護・Undoは検証済み。ただし単体成功を制作体験全体の成功とは扱わない。
- 全体目標は上記の実地検証まで継続。ユーザー固有の意図・好みの合否はユーザーテストで確認する。

## 2026-09-06 最終確認

- 吉川の確認済み修正を元の作品に適用（draft 968586f99b38）。元収録との音声相関で終盤を復旧、銘板の広すぎる領域を追従キーに変更、参照画像で作った部品説明画像と説明の切替を修正。修正前の全contentsはrecovery/before-reviewed-repair-1788674315.json。現在のユーザー編集との一致を確認してから反映した。
- 新規題材は「朝の珈琲店｜制作テスト」として吉川特装HP制作の部屋へ追加。4画像・本人声・5字幕。相談→生成→選択字幕の変更→実際のマイク入力経路で映像／音声／字幕を調整→1920x1080書き出しまで実行。テスト入力は録音音声。ユーザー本人による新UIの使い勝手の評価は未実施。
- 音声応答がincompleteで終わっても完了扱いする問題を修正。長さによる中断だけ続行し、継続回数を超えた場合は途中終了を表示。コンテンツフィルターによる中断は自動再試行しない。実ブラウザで継続・停止を確認。
- 速度変更は元の発話全体を残す方式に修正。音声とリンク映像が同じ終端を保持するテストに合格。
- 編集メニューからコピー・貼り付け・保存・操作一覧にアクセス可能。ショートカットだけに依存しない。
- Python48件合格、ネイティブ19件合格。実際のWebRTCで画像参照送信、音声から複合修正、テキストから書き出しを確認。
- 自動映像検品には誤認があった。フレーム実物・発話の実測・保存差分を照合して判断。任意の題材が一発で意図どおりになる保証や、理想体験100%達成の根拠にはしない。
- 追加発見: 静止画の描画が明示されたfit=coverを無視してcontainしていた。透過を保ってcover描画するよう修正し、2688x1520の素材を1920x1080へ表示した両端の画素が黒でないことを実測。プレビュー・書き出しは同じ処理。
- 最終テスト書き出し: uploads/editor-recovery-check/coffee-editor-test.mp4。参照用の検品レンダーは通常の書き出し一覧に登録しないよう修正。
- 全編検品で追加再現した日野ナンバーの84.8〜85.53秒の追従外れと、銘板86.3〜86.6秒の右端の漏れをキー修正。背景のナンバーも独立した領域クリップで処理。終盤167.2〜168.7秒のカーソルと操作表示は元の4Kファイル自体に含まれていたため、同じ元素材のきれいなフレームを参照する小さな映像クリップで補修した。字幕や元映像への焼き込みはしていない。比較フレーム確認後、現在の編集との一致を確認して適用。バックアップ: recovery/before-finishing-1788676268.json。
- 補修後の3分全編を再書き出し・音声込みで再検品（recovery/final-review/quality.txt）。部品説明との対応、終盤の重複解消、最後の発話の保持を確認。検品モデルの残る指摘は隣接する点検日シール・メーカー名・丸い検査シールで、車両識別情報の露出とは分けて判断した。全画面モザイクへ広げてはいない。末尾の余韻や制作テストの語り口など、演出の好みはユーザーの実地評価に残る。


## 2026-09-07 監視後の提案画面・会話修正

- 監視記録: uploads/editor-live-observation-20260907.json。ユーザーのマイクや制作セッションは再開せず、隔離テストで実装を確認。
- 提案はYouTube以外の動画区間、ライブラリ素材、画像、音声、文字、宣言形式の動く組合せを表示できる。文字・図形・元素材の動きはブラウザーで直接再生し、動画生成APIを呼ばない。1案を大きく表示、タブ切替と2案比較、画面高さへの追従、再生停止・シーク、縮小モーション設定に対応。
- 選んだ案の実物と感想、以前の案を作品に保存。表示はタイムラインを書き換えず、採用した案を通し下書きの指示へ渡す。
- 現在の予算は無料素材・コード・ローカル音声、必要な画像生成のみGPT Image 2。MCPの有料動画生成と他の画像モデルを無効化し、CLIにも同じ予算を渡す。
- 検索は候補情報を返し、目的に合う候補を選んでから提示。自動で検索先頭を推奨する動作を廃止。未確認の動画区間を観察済みと扱わない。
- 回答送信は再生位置・選択・pointerなどの一時的状態に依存しないリクエストへ変更。担当終了後も回答を保存し、次の制作指示へ引き継ぐ。終了直前に届いた質問も追跡中の仕事なら保持。
- 黙って待つ要求と必要な完了報告の停止を分離。古い誤った通知停止フラグを移行。追跡仕事・未通知結果を保存し、切断中の完了も再接続後に報告。部屋を変えたときの混在とプレイヤー残留を解消。
- モデル名や接続可否は調査を委譲できること、固有名詞を元資料で照合すること、既存回答を読み一問ずつ尋ねること、見せてと言われたら実物を作ることを会話・制作側で統一。待機判定だけの応答に英語の前置きが出る問題を確認し、判定では道具のみ呼ぶよう修正。
- 検証: 関連Pythonは39件（32件＋別途制作ライフサイクル7件）合格。実ブラウザーで動く案の再生・比較・採用・部屋切替、履歴・再接続・終了済み質問・切断中の結果保持を確認。実Realtimeで待機・再開・目的保持・調査委譲・実物の提案を確認。
- 実モデルが作った2案を実際に保存・表示するテストは、提案と応答まで13.52秒。採用から制作受付まで9.38秒。このテストの制作担当は受付を模擬しており、完成動画の品質や通し制作の成功を証明するものではない。画像・動画生成サービスは使用していない。
- 本体8000へ反映、公開ページと新しいスクリプト・スタイルの配信を確認。実音声サービスで完了報告を一度だけ発話する検証も合格。
- 未検証: 新しい提案から実際のAstra担当が作る全編の演出品質と、ユーザー本人の自然な会話での合否。モデルの返答にはまだ長い前置きが混ざることがあり、全ての会話問題が解消したとは扱わない。


## 2026-09-07 15:21のユーザーテスト後の修正

- 制作担当を短命の起動プロセス経由で独立実行。会話サーバーのプロセスツリー終了でも継続。下書きIDを仕事へ保存し、担当自体が停止した場合は起動時に同じ下書きから最大2回復旧。既に反映済みなら再制作しない。複数サーバーの復旧競合をロック。取消はその担当のプロセスと子だけを終了する。
- MCPの音声生成・画像生成・検品を別スレッドへ移し、待ち時間中の編集操作を可能にした。下書き変更は直列化し、生成後の素材登録時は最新の下書きを読む。GPU自体は引き続き1仕事ずつ。生存中のGPU処理のロックを経過時間だけで奪わない。
- Qwen音声生成は常駐サービスでモデルと声の特徴を再利用。同じ声・台詞の出力も再利用する。GPU未使用時は重みをRAMへ戻す。日本語の短文2例で6.69秒/11.05秒、同一台詞の再利用0.02秒。前回の長文生成との速度倍率比較には使えない。
- 完了・失敗の音声通知はRealtimeのout-of-band responseへ分離。過去の発言に返答する汚染を防ぎ、実際に喋った通知は会話記録へ残す。公式仕様: https://developers.openai.com/api/docs/guides/realtime-conversations
- editor_helpで画面の挙動・操作仕様・提案機能を必要時に読めるようにした。制作意図は登場人物や対比の内容を含めて保存するよう会話側へ伝達。毎回の確認や試作を必須にはしない。
- 検証: 関連Python60件合格。サーバー相当の親ツリーを実際に終了して独立担当の完了を確認。実Realtimeで過去にクリップの点滅を質問した履歴があっても、今回の再起動失敗だけを一度報告するテストに合格。
- 実制作: editor-durable-draft-6da96a6f / ef11c700-3dad-4954-a9bd-0414f5168b14。Astraが6秒、独立字幕2本＋Qwen音声を作成・音声認識で照合・通し検品・反映。別コピーeditor-astra-73a48539で字幕文言だけを変更し、他属性と他クリップが完全一致することを検証。これは機能テストであり、本編や演出品質の合格ではない。
- 上記6秒テストは約196秒。途中、Astraが字幕入りの画像を字幕なしと誤認し、追加検品に時間を使った。保存画像をこちらで見て字幕の存在を確認。確認ツールの出力に画像の実ファイルパスと対象字幕のデータを添え、疑義があれば画像を直接読み直せるようにした。モデルの見間違いや制作全体の速度が解消したとは扱わない。
- H3用RunPodは既存4台がEXITED。1台の起動要求が自動承認レビューで拒否された（理由の詳細なし）。起動・クラウド上の計測は未実施。既存環境の消去、新規GPU契約、有料動画生成は行っていない。
- 残る実地確認: 人物の対比等の実物の案を自然な会話から出す→ユーザーが採用→同じ方向性で通し下書き、という一連の演出・会話の合否。ユーザーの直前テストではYouTube以外の案の表示自体が未テスト。


## 2026-09-08 クラウド音声生成への切替

- ユーザーの起動操作後、RunPod移行先4drusrkcie2ob6（dan-h3-d-migration）へSSH接続。実機nvidia-smiでRTX4090/24564MiBを確認。既存ネットワークボリューム83de4ie9meのH3/ComfyUIは保持し、/workspace/dan-ttsへ独立環境を設置。
- app/services/cloud_speech.pyを追加。SSHチャネル経由でlocalhost音声サービスに接続し、声の参照素材をハッシュで再利用、WAVをローカルへ戻して通常の素材として登録する。cloud.jsonはuploads内の非公開実行設定でenabled=true。クラウド失敗時に黙ってローカル生成へ戻さない。停止済みGPUの課金起動は行わない。稼働GPU内のサービスだけは再起動時に再立ち上げできる。
- 前回22分14秒かかった同一台詞・同じowner参照・Qwen1.7Bの測定: クラウド生成23.22秒、転送を含む32.72秒、生成音声31.2秒。初回短文はライブラリ初期化等を含む97.89秒で、通常の生成時間とは分離。初回インストールは永続ディスクへの多数のファイル配置で約10分以上。モデル取得は別途並行で完了。
- ASRで長文の冒頭〜締めまで対応を確認。固有語等の認識表記揺れあり。声質の主観評価はユーザー評価と区別。
- 実エディターツール→クラウド→素材登録テスト cloud-voice-acceptance-87d49eac: 短文3.6秒の音声を生成3.22秒、転送・登録込み12.09秒。backend=cloud/pod_idが生成素材に保存され、下書きへ追跡登録されたことを検証。元のユーザー動画は変更していない。
- クラウド経路、キャッシュ再利用、障害時の明示エラー、GPU排他・並行処理の関連テスト5件合格。この変更は音声生成の高速化であり、会話・演出・タイムライン反映の未解決事項は別途残る。
