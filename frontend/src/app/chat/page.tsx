'use client';

/**
 * /chat - メインページ
 *
 * レイアウト（MainLayout）と認証チェックは app/chat/layout.tsx が担当する。
 * このページは URL を消費するだけで、実際の表示は MainLayout が
 * `selectedProjectId` ストアに基づいて行うため、null を返す。
 */
export default function ChatPage() {
  return null;
}
