# この部屋の音声note運用

room_id: e47f056e-77b1-4e39-9173-4a01dfadbc03
成果物: voice-note。保存先は既存の認証付き /api/v1/dan-notion/blocks。公開artifact_documentsには記事を保存しない。
root page: properties.kind=voice_note_workspace, room_id。子pageのproperties.articleにmodel.tsのArticleを保存。
DB設計とAPIは既存blocksと認証付きdan-notion APIを再利用する。新規の公開データAPIは設けない。

## 記事制作
録音停止後は「noteにする」で未保存の録音を保存してから制作を開始する。保存失敗時は録音を保持し、制作開始失敗時は保存済み音声を再利用する。
編集室の「noteにする」は `/api/voice-note/editorial` に所有者の認証を付けて送る。メインチャットの送信APIは使用しない。
非公開blocksのarticle.editorialに処理ID・状態・進捗を保存し、画面は2秒ごとに取得。文字起こし原文を先に保存し、文章生成はツールを持たない定額CLIで実行する。audioは保持し、transcribedAudioで再処理時の原文重複を防ぐ。本文不足は質問として表示。失敗は編集室内に表示し再試行可能。
稼働先は現在のPC上の編集室。Python・ffmpeg・Claude CLIはサーバー側に必要。外部公開用の実行先は未設定なので、外部サイトで動作確認済みとは扱わない。
1. 本人がこの部屋で自由に話した内容、指定音声、対象記事を取得。音声は実際に聞く/文字起こしする。元の音声URLをaudioに、原文をtranscriptに残す。
2. 本人の特徴的な話し方（一人称・語尾・例え・温度）を保って完成原稿を先に作る。構成案の事前選択は不要。体験・数字・一般知識・主張を補わない。
3. 全文を話題単位で読み、後出しの補足を該当箇所へ統合。重複と明らかな余談を削り、読みたくなりスムーズに読める順序へ並べ替える。本人らしさや理解に役立つ寄り道は残す。固定の問題解決型・エッセイ型を強制しない。
4. 実際の内容に合う読みたくなるタイトルを1つ選ぶ。free/paidはMarkdown、必要な見出しだけ##、太字は要点だけ。誇張や効果保証を加えない。
5. 一続きの原稿として完成させる。独自の具体的な判断材料が不足する場合はprice=0、全文free、paidは空。文字数で機械的に有料分割せず、有料直前の予告は原則なし。質問は事実・意味の確認が必要な場合だけ最大3つ、通常は空。比較試作時は変更前の原稿を非公開の記事として残す。
6. 保存済みのpropertiesを保持しarticleを更新、status=確認待ち。未解決事項はquestions。保存・表示まで確認。

## 投稿
公開は内容と価格を示した後の本人承認を受けて行う。編集画面の「この内容で投稿を依頼」が承認になる。
browserで実際の投稿先へopen_target、保存済み資格情報を確認してログイン。既存セッションを再利用。
2026-09-10: 本人が投稿先 https://note.com/mike_write を指定。以前のmickey_writerは誤り。ログイン後のアカウントがmike_writeと一致することを確認してから投稿する。画像認証は未解決。
noteの現在の画面を見てタイトル・本文・見出し・太字を入力し、価格とfree/paidの境界を有料ラインに設定する。独自の非公式投稿APIを推測して送信しない。
noteUrlがあれば二重投稿せず先に既存記事を確認。予定日時はAsia/Tokyo。未来の公開はwatchに対象記事ID・本文版・承認価格・日時を明記して登録する。
公開後、実ページのタイトル・本文・価格・無料範囲を確認。本人表示では有料全文が見えるので無料範囲は未ログイン表示でも確認。成功後だけstatus=公開済み、noteUrlを保存。

## 継続と売上
月30万円は目標で保証ではない。価格・記事数・購入数・売上・受取額を区別。画面に入れる実績はnoteの管理画面で確認した数字だけ。
salesMonthは対象月、checkedAtは確認日時。各記事の当月実績をpurchases/gross/receivedへ。月をまたぐとき過去月をproperties.monthly_historyに保存してから切り替える（画面経由は保存時に自動退避）。
テーマ・販売価格・継続頻度は素材と実績から改善する。記事のない日に架空の体験で埋めない。本人の希望頻度が決まったら録音案内・振り返りをwatchへ登録する。

録音は停止後に自動保存する。録音中は1秒ごとに端末のIndexedDBへ予備保存し、同じ本人の編集室・記事で復元。接続復帰時と30秒ごとに保存再試行。録音の個別削除は確認後に記事のaudioから除外し、既存の文字起こしと本文は保持する。

録音直後・保存済み音声の再生には共通のStableAudioを使う。全長が未確定のWebMは再生せず末尾を読み、全長を確定して先頭に戻してから操作を表示する。準備中は長さの確認表示、失敗時は再試行を表示。元の録音ファイルは変更しない。

## 2026-09-13 確定事項（以前の記載より優先）
- 語尾の終助詞「よ」「ね」「よね」は引用を含め記事に使わない。文字起こし原文は保持。
- 販売価格0でも、本文中に有料候補の区切りを必ず表示。価格0で投稿するときはfreeとpaidを連結した全文を無料公開する。区切りの表示自体は投稿本文へ転記しない。
- タイトル編集後は「このタイトルで本文を作り直す」。タイトルを固定して導入・構成・結末を整える。変更前の原稿はrevisionsに残し画面で読める。
- 図解は素材にある情報だけからPNGを作り、ファイル保存して本文へ挿入する。証拠画面を図解で捏造しない。
- ダン内の実チャット・修正記録など自分で取得可能な画像は積極的に用意する。本人へスクショ作成を任せない。相手の識別情報を隠し、拡大・抜粋した場合はキャプションに明示。
- 事前に用意した証拠画像はarticle.imagesにurl/captionで保存し、再生成時に引き継ぐ。
- 本文・画像・語尾・境界は実際の生成結果で検品する。noteへの投稿は別承認。

## 2026-09-13 本人による仕上げを優先
文章生成の指示は読者向け強化前（b379152の親）へ戻した。画像・有料境界・語尾禁止・タイトル固定は保持。
本人の言葉や意図を別の主張に変える改稿はしない。既存稿を無断で再生成しない。
画像は本文表示の「この画像を削除」で除去できる。元ファイルと履歴は残す。
有料ラインは全文編集欄のカーソル位置で変更できる。価格0でも表示する。
本人の完成原稿が得られるまでは新しい文章改善方針を重ねない。タイトルから一文ずつ次を読みたくなる流れと、本人の言葉を基準にする。画像は文章の再掲で埋めない。
