---
name: studio
description: >
  動画の企画・生成・編集を行うスキル。
  提案動画、デモ動画、紹介動画、チュートリアル、SNSショート等あらゆる種類に対応する。
  ユーザーが「動画」「映像」「ムービー」「スタジオ」「動画にして」と言った場合や、
  プロジェクトの計画に動画制作が含まれる場合に使用する。
display_name: ダンスタジオ
updated: 2026-04-12
---

# ダンスタジオ

動画の企画・生成・編集を行う汎用スキル。designスキルがUIを伴う成果物全般を扱うように、このスキルは動画を伴う成果物全般を扱う。

---

## 共通ルール

### 品質チェック

レンダリング後、ユーザーに提出する前にGemini APIで品質チェックする。ダン自身は動画を視聴できないため必ずGeminiに委託する。

**チェックプロンプトのルール:**
- 「合格/不合格で答えて」のような甘いプロンプトは禁止（全部合格になる）
- 「厳しく」「具体的なタイムスタンプ付きで」「カーソル表示箇所を全て列挙して意味があるか判定して」と指示する

**チェック項目:**
- [ ] 操作と画面遷移の因果関係が自然か？
- [ ] 字幕・ナレーション無しでも何をしているか伝わるか？
- [ ] BGMが映像と同じ長さで終わっているか？
- [ ] 動画の目的に対して必要なシーンが全て含まれているか？

1つでも不合格なら修正してから提出する。

### 音声

- **BGM**: 著作権フリーの実際の楽曲を使う。Pythonで生成したサイン波は禁止。映像の長さに合わせてトリムする
- **SE（効果音）**: 著作権フリーの実際の音源を使う。合成音禁止。Mixkit等からダウンロード
- まず字幕もナレーションも無しで内容が伝わる動画を目指す。非言語情報だけでは伝わらない場合に限り追加する

### その他

- **言語**: 指定がない限りユーザーと会話している言語に合わせる
- **動画の長さ**: 基本3分以内。尺に無理に収めて内容不足になることを避ける

---

## 制作技術

### フロー

1. Playwrightで操作録画（PNG連番）
2. カーソル合成（Pillowで後付け）
3. Remotionで編集（テキスト挿入、BGM、SE、トランジション）
4. `npx remotion render` でMP4出力

### 操作録画のルール

**要素の特定:**
- Playwrightは`query_selector`で実際のUI要素を見つけて操作する。座標の手書き・推測は禁止
- `bounding_box()`で取得した実座標を使う

**操作の範囲:**
- シーン設計書に書かれた操作だけを行う。設計書にない操作は入れない

**スクロール:**
- 瞬間移動（`window.scrollTo`で一気にジャンプ）は禁止。必ず滑らかなアニメーションスクロールを使う
- セクション移動は見出しを`query_selector`で見つけて`scroll_into_view`で移動する（Y座標の手書き禁止）
- スクロール判断はグループ単位で行う: これから操作する要素グループ全体が画面内に収まっているかを事前チェックし、収まっていればスクロールしない。個々の要素単位ではなく、操作グループ全体で判断する

**カーソル:**
- 意味のある操作時のみ表示（クリック、入力、ホバー）。無操作時は非表示
- 通常より大きく見やすいサイズ（28px）
- 録画時にmouse_positionsを記録し、Pillowで後付け合成

**SE用イベント記録:**
- 録画中にクリック・タイピング開始/終了のフレーム番号をevents.jsonに記録する
- Remotionでそのフレームにクリック音・タイピング音を`<Audio>`+`<Sequence>`で配置する

### Remotion編集

**テキスト挿入:** 操作デモの間に無地背景+テキストのシーンを挿入し、重要なメッセージを伝える
**BGM:** `<Audio>`でフェードイン/アウト付きで配置
**SE:** events.jsonのフレーム番号に基づいて`<Sequence>`+`<Audio>`で配置

### 汎用ツール（HTML録画方式）

| ツール | 用途 |
|--------|------|
| `studio_record` | HTMLをPlaywright headlessで録画 → .webm生成 |
| `studio_encode` | FFmpegで変換（WebM→MP4、解像度変更等） |
| `studio_probe` | 動画のメタ情報取得 |
| `studio_extract_frame` | 指定時刻のフレームをPNG抽出 |
| `send_file` | 完成動画をチャットUIに送信 |

**⚠️ Bash で Playwright/FFmpeg を直接実行しない。** 出力データがCLIメモリに蓄積してクラッシュする。

---

## パターン別ガイド

### パターン1: 提案動画

テキストで提案済みの内容を動画化する。

**前提:** リサーチ・要件策定・提案内容のテキスト化は完了済み。ユーザーから「動画にして」と指示されて起動。

**⚠️ 提案動画と承認の関係:** 動画はあくまで概要を視覚的に伝える補助資料。承認はテキストの詳細仕様書ベースで行う。動画だけで承認を取らない。

**フロー:**

1. **デモシーン抽出** — ユーザーの要望 + リサーチ結果の両方からデモで見せるべき項目を漏れなく抽出する
   - **「機能」**: 基本機能として明記されたもの
   - **「ビジョン」**: 将来構想として語られたもの
   - **「具体例」**: ユーザーが挙げた具体的な利用シーン
   - **「リサーチ発見」**: リサーチで発見した重要機能
   - **全項目に対してデモシーンを1つ作る。省略しない。**

2. **シーン設計書作成** — 抽出したシーンリストを物語構造に沿って構成する
   - `actions/video_guidelines.md` を参照
   - 各シーンには「何が映るか」「視聴者に何を感じさせるか」「前のシーンからどう繋がるか」を定義する
   - シーン設計書の全シーンを実装する。省略する場合は事前にユーザーに相談

3. **プロトタイプ作成** — 実際に操作可能なデモ用UIを構築
   - 実際にクリック・入力・遷移ができるWebページとして作る。HTMLにシーンを埋め込んでJSで切り替える方式は禁止
   - designスキルに従って作る（承認後にプロトタイプを拡張して本番化するため）
   - `frontend/src/app/demo/` 配下に作成
   - 動画提出時にプロトタイプの場所と主要ファイルを明記する（例: 「プロトタイプ: frontend/src/app/demo/documents/、主要ファイル: layout.tsx, page.tsx」）

4. **操作録画** — 上記「操作録画のルール」に従う

5. **編集** — Remotionでテキスト挿入・BGM・SE追加

6. **品質チェック** — 共通チェックリストに従う

**参考動画ライブラリ:**

| 用途 | URL |
|------|-----|
| SaaS紹介 | https://www.youtube.com/watch?v=mHUVjd8-BF8 |
| SaaS操作デモ | https://www.youtube.com/watch?v=CnAPjeQt5Jg |

### パターン2: 紹介動画（追加予定）

### パターン3: チュートリアル（追加予定）

### パターン4: SNSショート（追加予定）

---

## 学習パターン

`~/.dan/workspace/learned/studio/learned.md` を参照。
