# ネイティブエディター書き出し 引き継ぎ

## 目的

エディターのプレビューで見えている映像を、そのまま動画として高速に書き出せるようにする。

- テロップ、ぼかし、モザイク、追従ぼかし、ポップアウトの見た目と位置をプレビューと一致させる。
- チャットのDan経由と、エディターの「書き出し」ボタンを同じ書き出し経路に統一する。
- 数分の動画に数時間かかる現在の書き出しを改善する。

## ユーザーが確認した不具合

- 書き出し動画では一部テロップが消える。
- ぼかし位置がプレビューと異なる。
- モザイクの見た目がプレビューと異なる。
- つまり現行書き出しは使い物にならない。個別修正ではなく根本修正が必要。

## 現在の原因（確認済み）

プレビューと書き出しが別実装。

1. ネイティブエディターのD3D/GPU合成器
   - `D:\done-desktop\scripts\poc\production_desktop\native_ui\src\main.rs`
   - `compose()` がプレビューの映像、ぼかし、モザイク、追従マスク、ポップアウトを描画する。

2. WebViewのテロップ表示
   - 一部テロップはネイティブ合成器ではなくWebViewの前面レイヤーで描画される。

3. サーバー側FFmpeg書き出し
   - `D:\done\app\api\production_asset_routes.py` の `_render_sequence_job()`。
   - タイムラインを独自に再構成してFFmpegへ渡している。
   - 実際の今回の書き出しでは、128KBの巨大なfiltergraph、`movie=filename` 343本、overlay 177本、gblur 22本、audio segment 105本。
   - 5分32秒動画の書き出しに約3時間かかった。

この三者が別々に同じ見た目を再現しようとしているため、書き出し結果がプレビューと一致しない。

## 現在の書き出しボタン経路

- ネイティブUI `start_export()`（`main.rs` 約6904行）
- `POST /api/v1/production-assets/jobs` に `mode: export` とタイムラインを送る。
- バックエンドが `_render_sequence_job()` を呼び、現在はFFmpeg再現器で書き出す。

## 正しい実装方針

Danに「軽い方法を選ばせる」のではなく、書き出し基盤を強制的に置き換える。

1. ネイティブ合成器を画面なしで連続レンダリング可能にする。
2. そのフレーム列をFFmpegには圧縮だけさせる（FFmpegに映像効果を再現させない）。
3. テロップもネイティブ合成器の所有に統一する。WebViewだけに残すと、再び書き出しと一致しない。
4. 音声はタイムラインのリンク済み音声＋BGMを正しくミックスする。同じタイムラインを入力にする。
5. バックエンドのexport APIとネイティブUIの書き出しボタンの両方を新経路へ接続する。
6. 実動画を書き出し、複数時刻のプレビュー画像と書き出しフレームを比較してから完了報告する。

## 着手済み・未検証の変更

ネイティブUI `main.rs` に `--export-preview-video <out.mp4> [start] [end]` を追加した。

- `compose()` を各フレームに使い、RGBAをstdin経由でFFmpegへ渡して映像のみをエンコードする試作。
- まだ映像のみ。音声の統合、バックエンド接続、書き出しボタン接続、プレビュー比較は未実装。
- `cargo build --release` は成功。
- テストは実行できていない。現在動いている `C:\Users\Owner\.done\bin\native_ui.exe` が既存インスタンスとして新しいテストプロセスを終了させるため。
- 現在動いているエディターを勝手にkillしないこと。未保存編集を失う可能性がある。

## 注意

- 「できた」「復元した」と報告する前に、実際の再生／書き出し結果を必ず確認すること。過去に未確認で誤報したため、ユーザーは強く不信感を持っている。
- ユーザーは場当たり的なFFmpeg効果の修正を望んでいない。プレビューと書き出しを同一合成器へ統一する根本修正が必要。
- 完了条件は、エディターの書き出しボタンから動画を作り、プレビューと同一時刻のフレームでテロップ・ぼかし・モザイク・ポップアウトが一致することを確認すること。

## 直近の正常な旧書き出し（見た目は不一致なので納品不可）

`D:\done\uploads\production-assets\bd05fcc0-c143-4d1c-828e-7624e087b6c1\jobs\bff7bd77-11c8-428f-a0f2-edfc2bf886b8\bff7bd77-11c8-428f-a0f2-edfc2bf886b8_sequence.mp4`

