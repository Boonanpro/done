# ネイティブ移植ロードマップ（Web制作UI棚卸し 2026-07-04）
優先順（S/M/L=規模）:
1. 素材ライブラリ+取り込み(A1-A6): 一覧/サムネ/状態、パス登録、プロキシ、削除、紐付け — L
2. 生成開始(B1-B5): プリセット5種/ブリーフ/タイトル/形式→dan_plan→進捗イベント→開く — L
3. テロップ・デザイン(C2): プリセット10種+フォント/色/縁/箱/影/アニメ+全適用 — L
4. カット調整・再カット(C1): 無音しきい値/前後余白→recut — M
5. ぼかし/プライバシー(C4): 静止/追従(OCR)/キーフレーム — L
6. 保存/自動保存(E2): nativeは実装済(contents直書き) — 済
7. 書き出しジョブ+進捗+outputs(E1,E3): ボタン済、argv32K修正+イベント表示 — M
8. 指示クリップ&ダンに指示(C5,F1): dan_revise — M
9. クロップ(C6)+形状(C7) — S/M
10. 素材ドラッグ→タイムライン挿入(C12) — M (D&D取込は実装済、ライブラリからの投入)
11. 静止画フリーズ(C8) — S
12. 字幕音声同期(C3) — M
13. マーキー選択/+段/メモ(C9,C11,C14) — S
14. 飛び出しintensity/shadow調整+再試行(D3) — S
skip: D4(ブラウザalpha合成=native不要)
